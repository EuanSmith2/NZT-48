python-telegram-bot==22.8
httpx==0.28.1
python-dotenv==1.2.2
PyYAML==6.0.1
requests==2.34.2
anthropic==0.115.1
ddgs
textual

# --- optional: voice pipeline (voice.py) ---
# Only needed if you don't already have a whisper install that voice.py can
# find (it checks ~/.claude/mcp_servers/whisper-env/ first). Pulls torch (~2GB).
# openai-whisper==20250625
