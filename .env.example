# NZT-48 environment
# Copy to .env — install.sh fills these automatically

TELEGRAM_TOKEN=
TELEGRAM_USER_ID=
BRAVE_API_KEY=          # optional — enables news digest in brief
ANTHROPIC_API_KEY=      # leave empty — NZT-48 uses Claude Code CLI, not direct API
